
// Starter Code
#include <bits/stdc++.h>
using namespace std;
class Solution
{
public:
    int longest_subsequence(std::string &str1,std::string &str2)
    {
        int longest_value=0;
        // Write your code here without removing the existing code
        // the variable 'str1'and 'str2' contains string as an input.
        // modified the variable 'longest_value' contain the output of the program in  expression.
        return longest_value;
    }
};

// Backend Code
int main()
{
    std::string str1,str2;
    std::cin>>str1>>str2;
    Solution ob;
    std::cout << ob.longest_subsequence(str1,str2)<< std::endl;

    return 0;
}