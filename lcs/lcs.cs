using System;
public class Solution
{
	public  int longest_subsequence(string str1,string str2)
        {
        
	    int longest_value = 0;
        // Write your code here without removing the exising code
        // the variable 'str1' and 'str2' contains string as an input.
        // modified the variable 'longest_value' contain the output of the program in  expression.
        return longest_value;
		   
           
        }
         public static void Main(string[] args)
        {
	       
            string s1 = Console.ReadLine();
            string s2 = Console.ReadLine();
            Solution g= new Solution();
            int ans=g.longest_subsequence(s1,s2);
             Console.WriteLine(ans);
            
           
        }
}