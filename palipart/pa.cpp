
// Starter Code
#include <bits/stdc++.h>
using namespace std;
class Solution
{
public:
    int MinCut(std::string &str1)
    {
        int minimum_partition=-1;
        // Write your code here without removing the existing code
        // the variable 'str1' contains the string as input.
        // modified the variable 'minimum_partition' contain the output of the program.
        return minimum_partition;
    }
};

// Backend Code
int main()
{
    std::string str1;
    std::cin>>str1;
    Solution ob;
    std::cout<<(ob.MinCut(str1))<<std::endl;
   
    return 0;
}