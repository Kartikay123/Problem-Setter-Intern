#include <stdio.h>
//#include<stdio.h>
#include <stdlib.h>
#include <stdbool.h>
// #include <stdio.h>
#include <string.h>
// Starter Code
int MinCut(char str1[])
{
    int minimum_partition = -1;
    // Write your code here without removing the existing code.
    // the variable 'str1' contains the string as input.
    // modified the variable 'minimum_partition ' contain the output of the program in boolean format.
    return minimum_partition;
}

// Backend Code
int main()
{
    char str1[10000];
    scanf("%s", &str1);
    printf("%d",MinCut(str1));
    return 0;
}